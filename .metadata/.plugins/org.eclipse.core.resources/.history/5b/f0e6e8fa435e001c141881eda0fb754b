package Interaction;

// 판매자 Seller
// 변수로 int money(매출), int mango(재고)를 가집니다.
// 생성자에서 money는 초기값이 0이고, mango는 입력받도록 해주세요
// sellMango(int mango)를 이용해서 망고개수를 입력받으면
// 자신의 this.mango 는 갯수만큼 차감하고, this.money는
// 망고개수 *  만큼 증가합니다
// 망고가 모자라면 " 망고가 모자랍니다" 라고 하고 메서드를 종료합니다.
// showSeller()를 이용해 현재 소지금, 망고개수를 볼 수 있습니다.
// Buyer에서 망고개수를 확인 할 수 있도록 getMango() 게터만 하나 만들어주세요

public class Seller {
	
	private int money; // 매출
	private int mango; // 재고 
	
	public Seller(int mango) {
		this.money = 0;
		this.mango = mango;
		
	}
	public void sellMango(int mango) {
		this.mango -= mango;
		this.money += mango * 4000;
		
		}		




	}
	
	


